name: Django CI CD

on: 
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted
    strategy:
      matrix:
        python-version: [3.11.6]

    steps:
      - uses: actions/checkout@v3
      - name: Set up python ${{ matrix.python-version }}
        uses: actions/setup-python@v3
        with: 
          python-version: ${{ martix.python-version }}
      - name: Install Dependency
        run: |
          cd Server/global_computer 
          virtualenv env
          source env/bin/activate
          pip install -r requirements.txt mysqlclient
        env:
          NAME: "global_ecom" 
          USER: "jahidul"
          PASSWORD: "J@hidul42598475"
          HOST: "localhost"
          PORT: "3306"
          DEBUG: "True"
          DJANGO_SECRET_KEY: "django-insecure-wpx$@io&-dzyks&m=z%&uc=4b!qn*@6no$(^a(!qivf2jypr#z"
      